spring:
  application:
    name: shulker-srm
  datasource:
    url: jdbc:postgresql://localhost:5433/shulker_db
    username: shulker_user
    password: 228kluevlox
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080

